<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <!-- Suppress Tomcat HTTP request parsing errors (scanners sending binary/TLS on plain HTTP) -->
    <logger name="org.apache.coyote.http11.Http11Processor" level="WARN"/>

    <!-- Suppress Spring Security filter chain info disclosure -->
    <logger name="org.springframework.security.web.DefaultSecurityFilterChain" level="OFF"/>
</configuration>
